#!/bin/bash
echo "Open this script for details about the steps needed to install the SAM CLI."

# For details, please visit: 
# https://docs.aws.amazon.com/serverless-application-model/latest/developerguide/install-sam-cli.html
#
# These steps guide you throught the SAN CLI installation. You can uncomment them 
# and run this script (note the need for `sudo`), or you can copy-paste them into 
# a terminal on an EC2 instance.
#
# curl -L https://github.com/aws/aws-sam-cli/releases/latest/download/aws-sam-cli-linux-x86_64.zip -o aws-sam-cli-linux-x86_64.zip
# unzip aws-sam-cli-linux-x86_64.zip -d sam-installation
# sudo ./sam-installation/install
# which sam
# sam --version
